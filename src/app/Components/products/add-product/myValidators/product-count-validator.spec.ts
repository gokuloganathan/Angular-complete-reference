import { ProductCountValidator } from './product-count-validator';

describe('ProductCountValidator', () => {
  it('should create an instance', () => {
    expect(new ProductCountValidator()).toBeTruthy();
  });
});
